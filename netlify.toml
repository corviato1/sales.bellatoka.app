[build]
  command = "npm run build"
  publish = "build"
  environment = { SECRETS_SCAN_OMIT_KEYS = "ADMIN_PASSWORD", GENERATE_SOURCEMAP = "false" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
